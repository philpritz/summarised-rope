# 2026-06-24 — tombstone (not a session note)

The note here — an ADR fork on guide representation (list vs vector vs cons) — was
judged routine and removed in `a23b01f`, the commit that established *routine changes
live in the commits*. Its rationale lives in that commit's message.

This stub only keeps the disk in step with the `discussions/2026-06-24` references
still in the git log, so orientation needn't hunt for a folder the log names but disk
lacks.
